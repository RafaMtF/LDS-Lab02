export interface Automovel {
    idAutomovel: number; // O tipo deve ser 'number' ou 'string'
    matricula: string;
    ano: number; // Alterar para 'string' se você preferir armazenar o ano como texto
    marca: string;
    modelo: string;
    placa: string;
}